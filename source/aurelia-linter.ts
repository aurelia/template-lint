import {Options} from './options';
import {Config} from './config';
import {File} from './file';

export class AureliaLinter {

  /** 
   * initialise the linter and return all the processed files (initial)
   */
  init(config: Config): Promise<File>{

  }
  
  // change events... 
}
